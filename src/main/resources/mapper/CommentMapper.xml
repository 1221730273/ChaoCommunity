<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljc.chaocommunity.mapper.CommentMapper">

    <!-- 根据帖子ID分页查询评论 -->
    <select id="selectPageVoByPostId" resultType="com.ljc.chaocommunity.pojo.vo.CommentVO">
        SELECT c.id, c.post_id, c.user_id, c.parent_id, c.content, c.like_count,
               c.create_time, c.update_time,
               u.username, u.nickname, u.avatar,
               p.title AS post_title
        FROM post_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN post p ON c.post_id = p.id
        WHERE c.post_id = #{postId}
          AND c.deleted = 0
        <choose>
            <when test="sort == 'hot'">
                ORDER BY c.like_count DESC, c.create_time DESC
            </when>
            <otherwise>
                ORDER BY c.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 根据用户ID分页查询评论 -->
    <select id="selectPageVoByUserId" resultType="com.ljc.chaocommunity.pojo.vo.CommentVO">
        SELECT c.id, c.post_id, c.user_id, c.parent_id, c.content, c.like_count,
               c.create_time, c.update_time,
               u.username, u.nickname, u.avatar,
               p.title AS post_title
        FROM post_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN post p ON c.post_id = p.id
        WHERE c.user_id = #{userId}
          AND c.deleted = 0
        <choose>
            <when test="sort == 'hot'">
                ORDER BY c.like_count DESC, c.create_time DESC
            </when>
            <otherwise>
                ORDER BY c.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 分页查询所有评论（管理端） -->
    <select id="selectPageVoAll" resultType="com.ljc.chaocommunity.pojo.vo.CommentVO">
        SELECT c.id, c.post_id, c.user_id, c.parent_id, c.content, c.like_count,
               c.create_time, c.update_time,
               u.username, u.nickname, u.avatar,
               p.title AS post_title
        FROM post_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN post p ON c.post_id = p.id
        WHERE c.deleted = 0
        ORDER BY c.create_time DESC
    </select>

    <!-- 根据ID查询单条评论VO -->
    <select id="selectVoById" resultType="com.ljc.chaocommunity.pojo.vo.CommentVO">
        SELECT c.id, c.post_id, c.user_id, c.parent_id, c.content, c.like_count,
               c.create_time, c.update_time,
               u.username, u.nickname, u.avatar,
               p.title AS post_title
        FROM post_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN post p ON c.post_id = p.id
        WHERE c.id = #{id}
          AND c.deleted = 0
    </select>

    <!-- 查询某父评论下的所有子回复（按时间升序） -->
    <select id="selectVoByParentId" resultType="com.ljc.chaocommunity.pojo.vo.CommentVO">
        SELECT c.id, c.post_id, c.user_id, c.parent_id, c.content, c.like_count,
               c.create_time, c.update_time,
               u.username, u.nickname, u.avatar,
               p.title AS post_title
        FROM post_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN post p ON c.post_id = p.id
        WHERE c.parent_id = #{parentId}
          AND c.deleted = 0
        ORDER BY c.create_time ASC
    </select>

</mapper>
