<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ljc.chaocommunity.mapper.PostMapper">

    <!-- ===== 首页分页查询：分类 + 排序（new/hot/follow）一个SQL搞定 ===== -->
    <select id="selectPageVo" resultType="com.ljc.chaocommunity.pojo.vo.PostVO">
        SELECT p.id, p.user_id, p.category_id, p.title, p.content,
               p.view_count, p.like_count, p.comment_count, p.cover_url, p.top, p.status, p.is_featured,
               p.create_time, p.update_time,
               u.username, u.nickname, u.avatar,
               c.name AS category_name
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        <where>
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="currentUserId != null">
                AND p.user_id IN (
                    SELECT followee_id FROM user_follow WHERE follower_id = #{currentUserId}
                )
            </if>
            AND p.deleted = 0
        </where>
        <choose>
            <when test="sort == 'hot'">
                ORDER BY p.view_count DESC, p.create_time DESC
            </when>
            <otherwise>
                ORDER BY p.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- ===== 用户主页：按用户ID + 排序（new/hot） ===== -->
    <select id="selectPageVoByUserId" resultType="com.ljc.chaocommunity.pojo.vo.PostVO">
        SELECT p.id, p.user_id, p.category_id, p.title, p.content,
               p.view_count, p.like_count, p.comment_count, p.cover_url, p.top, p.status, p.is_featured,
               p.create_time, p.update_time,
               u.username, u.nickname, u.avatar,
               c.name AS category_name
        FROM post p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.user_id = #{userId}
          AND p.deleted = 0
        <choose>
            <when test="sort == 'hot'">
                ORDER BY p.view_count DESC, p.create_time DESC
            </when>
            <otherwise>
                ORDER BY p.create_time DESC
            </otherwise>
        </choose>
    </select>

</mapper>
